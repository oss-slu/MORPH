[project]
name = "robot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dbus-fast>=4.0.4",
]

[dependency-groups]
dev = [
    "stickytape>=0.2.1",
]
